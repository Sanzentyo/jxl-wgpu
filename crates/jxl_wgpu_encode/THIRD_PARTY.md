# Third-party notices

Parts of `src/prefix.rs` and `src/lossless_modular.rs` are adapted from the
`zune-jpegxl` 0.5.2 fast-lossless encoder. The original is copyright the zune-image
developers and is used under the MIT license reproduced in
`LICENSES/zune-jpegxl-MIT.txt`.

The ANS alias-table construction and histogram grammar in `src/ans.rs` follow
libjxl's `ans_common.cc` and `enc_ans.cc`, copyright the JPEG XL Project Authors,
under the BSD license reproduced in `LICENSES/libjxl-BSD.txt`.
